[tools]
taplo = "latest"
prettier = "latest"

[tasks.format]
run = [
  "find .fish -type f -name '*.fish' -exec fish_indent -w {} +",
  "taplo format '*.toml' '.*/*.toml'",
  "prettier --write '**/*.json' '!.pi/projects/**'",
]
